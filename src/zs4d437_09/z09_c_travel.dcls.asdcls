@EndUserText.label: 'Access control'
@MappingRole: true
define role Z09_C_TRAVEL {
    grant
        select
            on
                Z09_C_TRAVEL
                    where
                        inheriting conditions from entity Z09_R_Travel;
                        
}